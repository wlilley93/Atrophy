/**
 * Config values from main process.
 */

export const settings = $state({
  userName: 'User',
  version: '0.0.0',
  avatarEnabled: false,
  ttsBackend: 'elevenlabs',
  inputMode: 'dual',
  loaded: false,
});
