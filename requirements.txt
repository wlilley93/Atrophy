# Voice
sounddevice>=0.5.0
numpy
pynput

# TTS (ElevenLabs)
httpx
elevenlabs

# Embeddings (semantic memory) — installed lazily on first use
# sentence-transformers

# Avatar / Video
opencv-python
ffmpeg-python

# Display
PyQt5
PyQt5-sip

# HTTP API (--server mode)
flask

# Icons
python-lucide

# Utilities
python-dotenv
rich
defusedxml

# Google integration (optional — Gmail + Calendar)
google-auth-oauthlib
google-api-python-client
google-auth-httplib2

# Face generation (optional — only for scripts/generate_face.py)
# replicate
